import { FormatGenrePipe } from './format-genre.pipe';

describe('FormatGenrePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatGenrePipe();
    expect(pipe).toBeTruthy();
  });
});
