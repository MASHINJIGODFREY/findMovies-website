import { FormatLanguagesPipe } from './format-languages.pipe';

describe('FormatLanguagesPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatLanguagesPipe();
    expect(pipe).toBeTruthy();
  });
});
