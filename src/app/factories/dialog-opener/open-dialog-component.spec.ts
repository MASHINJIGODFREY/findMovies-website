import { OpenDialogComponent } from './open-dialog-component';

describe('OpenDialogComponent', () => {
  it('should create an instance', () => {
    expect(new OpenDialogComponent()).toBeTruthy();
  });
});
