import { FormatCountriesPipe } from './format-countries.pipe';

describe('FormatCountriesPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatCountriesPipe();
    expect(pipe).toBeTruthy();
  });
});
